# FoodSpec Prediction SPA (Placeholder)

This folder is a scaffold for a lightweight React/Vue SPA:
- Drag/drop CSV/HDF5 upload
- Model selector (populated from /models)
- Predict button calls /predict and renders results + plots

Set `FOODSPEC_MODELS_DIR` on the backend to point to frozen models.
