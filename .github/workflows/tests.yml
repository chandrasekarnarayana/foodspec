name: CI (Legacy - Use ci.yml)

# This workflow is deprecated. All CI logic has been moved to ci.yml.
# This file is kept for reference only and will be removed in future versions.
# GitHub automatically runs ci.yml instead since it has the same triggers.

on:
  workflow_dispatch:

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: Note
        run: |
          echo "This workflow is deprecated. Please use ci.yml instead."
          echo "All CI checks have been consolidated into a single, optimized workflow."

