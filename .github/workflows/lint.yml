name: Lint (Legacy - Use ci.yml)

# This workflow is deprecated. Linting is now part of ci.yml.
# This file is kept for reference only.

on:
  workflow_dispatch:

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: Note
        run: |
          echo "This workflow is deprecated. Linting is now part of ci.yml."
          echo "Run: git push or create a PR to trigger the full CI pipeline."

